{
  "id": "managed-kafka-staging",
  "config": {
    "grafana": {
      "dashboards": []
    },
    "alertmanager": {
      "pagerDutySecretName": "redhat-rhoam-pagerduty",
      "deadmansSnitchSecretName": "redhat-rhoam-deadmanssnitch"
    },
    "prometheus": {
      "pod_monitors": [
      ],
      "rules": [
      ],
    },
  }
}